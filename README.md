# My slstatus build
Patches used:
- none

## Installation
```
git clone https://github.com/DefinitelyNotMai/slstatus
cd slstatus
sudo make install
```

## Configuration
Configure slstatus by making changes in "config.h", run "sudo make install" afterwards.
